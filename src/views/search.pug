extends base
include mixins/video

block content 
  form 
    input(placeholder="Search by title", name="keyword", required)
    input(type="submit", value="Search now")
  each video in videos 
    +video(video)